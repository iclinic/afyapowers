# Project Context: {{feature_name}}

<!-- Generated during the design phase. Consumed by plan, implement, and complete phases.
     If this file doesn't exist, downstream phases fall back to their own exploration. -->

## Project Structure

<!-- Key directories and their purpose. Naming conventions for files/modules.
     Example: "src/components/ — React components, PascalCase filenames" -->

## Code Patterns

<!-- Real examples from the codebase showing: import style, module structure,
     error handling patterns, export conventions. Keep to 3-5 representative snippets
     (under 10 lines each) from the area where changes will happen. -->

## Testing Patterns

<!-- Test framework (jest, vitest, pytest, etc.), file naming convention,
     test structure (describe/it, AAA pattern, etc.), mock usage patterns,
     test run commands. -->

## Build & Run Commands

<!-- How to build, run dev server, run tests, run linter. -->

## Import Conventions

<!-- How imports are organized: path aliases (e.g., @/), barrel files,
     relative vs absolute imports, import grouping order (external, internal, types). -->

## Reusable Patterns & Examples

<!-- Existing features, modules, or flows in the codebase that are similar to what
     this feature needs to build. For each reference, include:
     - What it is and where it lives (file path)
     - Why it's relevant (what aspect to reuse or follow as a model)
     - The ACTUAL CODE — embed enough of the file to serve as a self-contained
       reference (full functions, full components, full test cases).
       Downstream phases will NOT read these files — this is their only source.
       Aim for 30-80 lines per reference; go higher if it loses meaning truncated.

     Examples: an existing API endpoint that follows the same CRUD pattern,
     a component that handles similar state management, a test file that
     demonstrates the project's integration testing approach, a service that
     already implements the same auth/validation flow.

     Focus on references directly relevant to the current feature — not generic
     project patterns (those belong in Code Patterns above). -->

## Commit Conventions

<!-- Message format, common types, scope usage, ticket ID from branch name,
     pre-commit hooks, commitlint config, 3-5 real examples from git log.
     Include retry instructions for hook failures. -->

## Framework & Component Detection

<!-- Only for UI/Figma features. Remove this section entirely if not applicable.
     Detected framework, component directory path, Storybook presence. -->
